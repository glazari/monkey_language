1 + 5 + 3;

let a = 40;
let wildfire = 89;

if (true) { 10 } else {20};

if (true) { 
    10 
} else {
    20
};

let fib = fn (n) {
    if (n < 2) {
        return 1
    }
    return fib(n-1) + fib(n-2)
};

fib(0);
fib(1);
fib(2);
fib(3);
fib(4);
fib(5);
fib(6);
fib(7);
fib(8);
